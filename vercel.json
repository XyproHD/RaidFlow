{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "npm run build",
  "installCommand": "npm cache clean --force && npm ci --prefer-online && npx prisma generate"
}
